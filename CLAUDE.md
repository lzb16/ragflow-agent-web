# Claude Code 项目指南

## MCP 工具使用时机

### 1. `mcp__auggie__codebase-retrieval` — 代码库检索

**首选代码搜索工具**，优先于 Grep/Glob 使用于以下场景：

| 场景 | 示例 |
|------|------|
| 不知道功能在哪个文件里 | "用户认证逻辑在哪里实现的？" |
| 需要了解某个模块的整体结构 | "API 层是怎么组织的？" |
| 跨文件查找相关代码 | "哪些地方处理了表单提交？" |
| 执行任务前收集上下文 | "当前路由配置是什么样的？" |

**不要用于：**
- 查找某个已知标识符的所有引用 → 用 Grep
- 查看某个具体文件内容 → 用 Read
- 查找特定类/函数定义 → 用 Grep

---

### 2. `grok-search` — 网络搜索（Grok + Tavily）

用于需要**外部实时信息**的场景：

| 场景 | 示例 |
|------|------|
| 查询库的最新版本或 API | "React Router v7 的用法" |
| 查找第三方文档或示例 | "Tailwind CSS v4 配置方法" |
| 调查未知错误或问题 | "某个 npm 包的 breaking change" |
| 获取行业最佳实践 | "RAGFlow API 接口文档" |

**不要用于：** 本地代码库相关问题（用 auggie）。

---

## 通用原则

- 代码库问题 → `auggie` 优先
- 外部知识/文档 → `grok-search`
- 确定文件路径后 → `Read` / `Grep` / `Glob`
